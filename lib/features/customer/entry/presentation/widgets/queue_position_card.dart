import 'package:flutter/material.dart';

/// Displays a customer's current queue position and status.
///
/// Full implementation with live data is added in Phase 4 (T025).
class QueuePositionCard extends StatelessWidget {
  const QueuePositionCard({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(T025): Implement with live CustomerQueueStatusView data (Phase 4).
    return const SizedBox.shrink();
  }
}
